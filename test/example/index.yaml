
servers:
- url: https://example.com

paths:
  /user:
    get:
      summary: List user
      responses:
        '404':
          # description: Not found
          content:
            text/plain:
              schema:
                type: string
                default: "Not found"

