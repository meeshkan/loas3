import { $LazyLazyParams, $LazyParams } from "../generated/lazy";
import { Parameter } from "../generated/full";

export const _lazylazy = (o: $LazyLazyParams): Parameter[] => [];

export const _lazy = (o: $LazyParams): Parameter[] => [];
